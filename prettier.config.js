/** @type {import('prettier').Config} */
const config = {
  plugins: ['prettier-plugin-tailwindcss'],
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  printWidth: 100,
};

module.exports = config;
